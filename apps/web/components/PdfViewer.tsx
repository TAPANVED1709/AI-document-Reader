'use client';
import { API_BASE_URL } from '../lib/api';
export default function PdfViewer({ reportId, page, setPage }: { reportId: string; page: number; setPage: (page: number) => void }) { return <section className="rounded-xl border border-line bg-white p-3 shadow-sm lg:sticky lg:top-4 lg:h-[calc(100vh-2rem)]"><div className="flex items-center justify-between px-2 py-2"><h3 className="font-bold">Original PDF</h3><label className="text-xs text-slate-500">Page <input aria-label="PDF page" type="number" min="1" value={page} onChange={e => setPage(Math.max(1, Number(e.target.value)))} className="w-12 rounded border border-line px-1 py-0.5 text-center" /></label></div><iframe title="Original laboratory report" src={`${API_BASE_URL}/api/reports/${reportId}/file#page=${page}`} className="h-[calc(100%-3rem)] min-h-[520px] w-full rounded-lg border border-line" /></section>; }
